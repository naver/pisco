[project]
name = "pisco"
version = "1.0.0"
requires-python = ">=3.10,<3.14"
dependencies = [
    "torch>=2.6",
    "transformers>=4.0",
    "peft>=0.15",
]

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build.targets.wheel]
packages = ["pisco"]